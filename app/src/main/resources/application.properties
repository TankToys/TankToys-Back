# server config
server.port=8090

# api config
api-version-path=/api/v1

# api resource paths
userPath=${api-version-path}/user
roomPath=${api-version-path}/room
rankingPath=${api-version-path}/ranking
tankPath=${api-version-path}/tank
mapPath=${api-version-path}/map

# api multiplayer paths
multiplayerPath=${api-version-path}/multi

# database
jdbcUrl=url
spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/
spring.datasource.username=tanktoys
spring.datasource.password=1234